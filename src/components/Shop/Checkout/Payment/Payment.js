import Input from '../../../UI/Input/Input';
import Button from '../../../UI/Buttons/Button/Button';

const Payment = _ => {
	return (
		<div>
			<form>
				{/* <Input name='cardnumber' title='Card number' type='text' /> */}
				<Button>Pay</Button>
			</form>
		</div>
	);
};

export default Payment;
